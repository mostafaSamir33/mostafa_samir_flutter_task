import 'package:flutter/material.dart';

class BouquetCard extends StatelessWidget {
  const BouquetCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
